[project]
name = "mcp-boilerplate"
version = "0.1.0"
description = "A simple MCP server boilerplate"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=3.0.0",
]

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
